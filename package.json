{
  "name": "adr-guard",
  "version": "0.1.0",
  "description": "Agent Detection & Response — tool-agnostic detection engine that intercepts coding-agent tool calls before execution",
  "type": "module",
  "bin": {
    "adr": "dist/cli/index.js",
    "adr-guard": "dist/cli/index.js",
    "adr-hook": "dist/adapters/claude-code/hook-entry.js"
  },
  "files": [
    "dist",
    "rules",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src test",
    "format": "prettier --write .",
    "predemo": "npm run build",
    "demo": "node scripts/demo.mjs"
  },
  "license": "MIT",
  "dependencies": {
    "js-yaml": "^5.4.2"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^22.20.3",
    "eslint": "^10.10.0",
    "prettier": "^3.9.7",
    "ts-node": "^10.9.2",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.70.0",
    "vitest": "^5.0.1"
  }
}
